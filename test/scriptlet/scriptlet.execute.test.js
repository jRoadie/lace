'use strict';

var assert = require('assert');

var
    fs = require('fs'),
    cheerio = require('cheerio'),
    scriptlet = require('../../src/scriptlet')
    ;



describe('Scriptlet', function() {
    describe('.execute()', function () {
        it('should not pass if failed', function () {
            assert.equal()
        });
    });
});
